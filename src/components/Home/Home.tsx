import { FC } from 'react';

import classes from './Home.module.css';

const Home: FC = () => {
  return (
    <section className={classes.home}>
      <h1>Home Page</h1>
    </section>
  );
};

export default Home;
